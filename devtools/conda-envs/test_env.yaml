name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - openbabel


    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - rdkit-pypi
    - aqme
    - MDAnalysis

