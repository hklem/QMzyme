# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:python:.
version: 2.1
orbs:
  python: circleci/python@2.1.1
  codecov: codecov/codecov@4.0.1
jobs:
  build-and-test:
    # Install dependencies and run tests
    docker:
      #- image: cimg/python:3.11-node
      - image: continuumio/miniconda3
    #filters:
      #branches:
        #only:
          #- class_structure_redesign
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - run: |
          conda update -y conda
          conda create -n qmzyme python=3.11
          source activate base 
          conda activate qmzyme
          pip install -e .
          pip install pytest
          pip install pytest-cov
          pytest -v --color yes --cov-config=pyproject.toml --cov=QMzyme --cov-report=html 
      - store_artifacts:
          path: htmlcov
            
workflows:
  build-and-test:
    jobs:
      - build-and-test
